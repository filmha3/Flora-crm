# Flora — مسیر انتشار اپ

خلاصه‌ی وضعیت واقعی، بدون خوش‌بینی الکی.

---

## وضعیت فعلی

Flora یک **PWA** است (وب‌اپ نصب‌شدنی). همین الان روی آیفون و اندروید نصب می‌شود و تقریباً مثل اپ نیتیو کار می‌کند: آیکون روی صفحه‌ی اصلی، تمام‌صفحه بدون نوار مرورگر، کار آفلاین، نوتیفیکیشن، دوربین.

**این یعنی الان هم قابل استفاده است** — نیازی به اپ استور برای شروع کار نداری.

---

## مانع اصلی برای اپ استور

**Apple Developer Program از ایران قابل ثبت‌نام نیست.** به‌خاطر تحریم‌های آمریکا:
- ثبت‌نام با هویت/آدرس ایرانی رد می‌شود
- کارت بانکی ایرانی برای ۹۹ دلار سالانه کار نمی‌کند
- حتی با VPN هم مدارک هویتی بررسی می‌شود

این با کد قابل حل نیست. راه‌های واقعی:

| راه | هزینه | سختی | نتیجه |
|---|---|---|---|
| PWA (همین حالا) | رایگان | ندارد | کار می‌کند، ولی در اپ استور نیست |
| کافه‌بازار / مایکت | رایگان | کم | فقط اندروید، بازار ایران |
| حساب اپل از کشور دیگر | ۹۹ دلار/سال + مک | زیاد | اپ استور جهانی |

---

## گزینه ۱ — کافه‌بازار / مایکت (پیشنهاد اول)

بازار اصلی تو ایران است و اکثر مشاورهای املاک اندروید دارند.

**نیاز:** فقط یک کامپیوتر ویندوز/لینوکس. مک لازم نیست.

**مراحل:**
1. `npm install @capacitor/core @capacitor/cli @capacitor/android`
2. `npx cap init` (تنظیمات از `capacitor.config.json` که آماده کردم خوانده می‌شود)
3. `npm run build && npx cap add android && npx cap sync`
4. `npx cap open android` → در Android Studio یک APK/AAB امضاشده بساز
5. در [pishkhan.cafebazaar.ir](https://pishkhan.cafebazaar.ir) حساب بساز و آپلود کن

**زمان تقریبی:** یک تا دو روز کاری برای بار اول.

---

## گزینه ۲ — اپ استور (وقتی حساب پیدا کردی)

**نیاز:** مک با Xcode + حساب Apple Developer غیرایرانی.

**مراحل:**
1. همان مراحل ۱–۳ بالا، ولی `@capacitor/ios` به‌جای android
2. `npx cap add ios && npx cap open ios`
3. در Xcode: Bundle ID، امضا، و آیکون‌ها را ست کن
4. Archive → Upload to App Store Connect

**⚠️ ریسک رد شدن:** اپل طبق قانون ۴.۲ اپ‌هایی که فقط وب‌سایت داخل WebView هستند را رد می‌کند. برای عبور، Flora باید از پلاگین‌های نیتیو واقعی استفاده کند — نه فقط API مرورگر:

- `@capacitor/camera` به‌جای `<input type="file">` برای عکس ملک
- `@capacitor/push-notifications` به‌جای Web Push
- `@capacitor/geolocation` برای موقعیت ملک
- `@capacitor/share` برای اشتراک بکاپ و فایل

این تغییرات کد لازم دارد و باید وقتی واقعاً به مک رسیدی انجام شود — الان انجام دادنشان بی‌فایده است چون قابل تست نیست.

---

## چیزهایی که برای هر دو گزینه لازم است

**۱. سیاست حریم خصوصی (اجباری)**
هر دو فروشگاه لینک عمومی می‌خواهند. باید بگوید:
- چه داده‌ای جمع می‌شود (فایل ملک، مشتری، عکس، شماره تماس)
- کجا ذخیره می‌شود (Supabase، سرور در ژاپن)
- با چه کسی به اشتراک گذاشته می‌شود (هیچ‌کس؛ فقط سرویس‌های AI که خود کاربر کلیدش را وارد می‌کند)
- چطور حذف می‌شود

**۲. حساب تست برای بررسی‌کننده**
هر دو فروشگاه یک نام کاربری/رمز می‌خواهند تا اپ را تست کنند. یک حساب با داده‌ی نمونه بساز.

**۳. اسکرین‌شات‌ها**
اپ استور: ۶.۷ و ۶.۵ اینچ. کافه‌بازار: چند سایز اندروید.

**۴. توضیحات فروشگاه**
عنوان، توضیح کوتاه، توضیح کامل، کلمات کلیدی — به فارسی.

**۵. ⚠️ مسئله‌ی کلید هوش مصنوعی**
الان کاربر باید خودش کلید Gemini/AvalAI بگیرد. برای یک اپ عمومی این تجربه‌ی بدی است و ممکن است باعث رد شدن شود ("اپ بدون تنظیمات اضافه کار نمی‌کند"). قبل از انتشار عمومی باید تصمیم بگیری:
- یا کلید خودت را سمت سرور بگذاری و هزینه‌اش را بدهی
- یا بخش‌های AI را برای نسخه‌ی فروشگاهی اختیاری/مخفی کنی

---

## پیشنهاد من

۱. **الان:** PWA را بین چند مشاور آشنا پخش کن. بازخورد بگیر. رایگان و فوری است.
۲. **بعد:** اگر جواب داد، کافه‌بازار — بازار واقعی‌ات همان‌جاست.
۳. **آخر:** اپ استور، فقط اگر مخاطب خارج از ایران پیدا کردی که ارزش هزینه و دردسرش را داشته باشد.

ساختن نسخه‌ی اپ‌استور قبل از اینکه بدانی کسی از اپ استفاده می‌کند، ترتیب اشتباهی است.
